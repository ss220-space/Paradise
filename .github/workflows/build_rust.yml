name: Build and verify Rust library

on:
  issue_comment:
    types: [created]

jobs:
  build-and-check:
    permissions:
      contents: write
      pull-requests: write

    if: |
      github.event.issue.pull_request &&
      github.event.comment.body == '!build_rust' &&
       (github.event.comment.author_association == 'COLLABORATOR' ||
       github.event.comment.author_association == 'MEMBER' ||
       github.event.comment.author_association == 'OWNER')

    runs-on: ubuntu-latest

    steps:
    - name: Install jq
      run: sudo apt-get update && sudo apt-get install -y jq

    - run: echo "FAIL_NOTIFIED=false" >> "$GITHUB_ENV"

    - name: Like the comment
      env:
        BASE_REPOSITORY: ${{ github.repository }}
        GH_TOKEN: ${{ github.token }}
      run: |
        gh api \
          --method POST \
          -H "Accept: application/vnd.github+json" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          /repos/$BASE_REPOSITORY/issues/comments/${{ github.event.comment.id }}/reactions \
          -f content='+1'

    - name: Extract PR info
      id: pr_info
      run: |
        PR_API_URL="${{ github.event.issue.pull_request.url }}"
        PR_JSON=$(curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" "$PR_API_URL")
        echo "PR_REPO=$(echo "$PR_JSON" | jq -r '.head.repo.full_name')" >> $GITHUB_ENV
        echo "PR_BRANCH=$(echo "$PR_JSON"  | jq -r '.head.ref')"           >> $GITHUB_ENV
        echo "base_repo=$(echo "$PR_JSON" | jq -r '.base.repo.full_name')" >> $GITHUB_OUTPUT

    - name: Checkout PR branch
      uses: actions/checkout@v6
      with:
        repository: ${{ env.PR_REPO }}
        ref:          ${{ env.PR_BRANCH }}
        token:        ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 1

    - name: Setup Rust targets
      run: |
        rustup target add i686-unknown-linux-gnu
        rustup target add i686-pc-windows-gnu

    - name: Install cross-toolchain deps
      run: |
        sudo dpkg --add-architecture i386
        sudo apt-get update
        sudo apt-get install -y \
          zlib1g-dev:i386 \
          lib32gcc-11-dev \
          gcc-multilib g++-multilib \
          libc6-dev-i386 \
          libgcc-11-dev:i386 \
          mingw-w64 mingw-w64-i686-dev

    - name: Build Rust libraries
      env:
        BASE_BRANCH: ${{ github.event.repository.default_branch }}
        BASE_REPOSITORY: ${{ github.repository }}
        USER_USER: ${{ github.actor }}
        USER_EMAIL: ${{ github.event.sender.email }}
        SOURCE_DATE_EPOCH: "1577836800"
        RUSTFLAGS: "-C link-arg=-Wl,--build-id=none"
        TZ: UTC
      run: |
        git config --local user.name "Rust Builder"
        git config --local user.email "action@github.com"

        git pull origin "$PR_BRANCH" --depth=$((ahead_by + 1))
        git remote add upstream "https://github.com/$BASE_REPOSITORY.git"
        git fetch upstream "$BASE_BRANCH" --depth=$((behind_by + 1))

        cd rust

        WINDOWS_TARGET="i686-pc-windows-gnu"
        LINUX_TARGET="i686-unknown-linux-gnu"

        # Handle building on windows
        if [[ "$OSTYPE" == "msys" ]]; then
          WINDOWS_TARGET="i686-pc-windows-msvc"
        fi

        cargo build --release --target $LINUX_TARGET
        cp target/$LINUX_TARGET/release/librustlibs.so ../librustlibs_515.so
        cp target/$LINUX_TARGET/release/librustlibs.so ../librustlibs.so
        cargo clean
        cargo build --release --target $LINUX_TARGET --no-default-features --features byond-516
        cp target/$LINUX_TARGET/release/librustlibs.so ../librustlibs_516.so

        cargo build --release --target $WINDOWS_TARGET
        cp target/$WINDOWS_TARGET/release/rustlibs.dll ../rustlibs_515.dll
        cp target/$WINDOWS_TARGET/release/rustlibs.dll ../rustlibs.dll
        cargo clean
        cargo build --release --target $WINDOWS_TARGET --no-default-features --features byond-516
        cp target/$WINDOWS_TARGET/release/rustlibs.dll ../rustlibs_516.dll

        export RUSTFLAGS='-C target-cpu=raptorlake'
        cargo build --release --target=$LINUX_TARGET
        cp target/$LINUX_TARGET/release/librustlibs.so ../librustlibs_515_prod.so
        cargo clean
        cargo build --release --target=$LINUX_TARGET --no-default-features --features byond-516
        cp target/$LINUX_TARGET/release/librustlibs.so ../librustlibs_516_prod.so

    - name: Commit and push changes
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        git add --force librustlibs.so rustlibs.dll librustlibs_515.so librustlibs_516.so rustlibs_515.dll rustlibs_516.dll
        git commit -m "Build Rust library"
        git push origin

    - name: Notify Failure
      env:
        GH_TOKEN: ${{ github.token }}
      if: failure() && env.FAIL_NOTIFIED != 'true'
      run: |
        gh pr comment ${{ github.event.issue.html_url }} -b 'Building Rust library failed, see the action run log for details: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}'
